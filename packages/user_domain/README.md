# user_domain

Pure Dart package defining the **domain layer** for user management.

No Flutter dependency — contains only models, interfaces and use cases.

## Contents

- `User` model
- Repository interfaces (Auth, User)
- Use cases (SignIn, SignUp, SaveProfile)
