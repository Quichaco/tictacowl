name: user_domain
description: User domain models, repositories and datasource interfaces.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.10.5
  freezed: ^3.2.3
  json_serializable: ^6.9.0
