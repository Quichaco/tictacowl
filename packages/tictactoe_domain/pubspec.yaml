name: tictactoe_domain
description: Pure Dart package for XO Game domain logic.
publish_to: 'none'
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  freezed_annotation: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.5
  lints: ^6.0.0
  test: ^1.26.3
