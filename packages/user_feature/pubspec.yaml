name: user_feature
description: User authentication and profile feature with Firebase.
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.8

flutter:
  generate: true

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  hooks_riverpod: ^3.1.0
  flutter_hooks: ^0.21.3+1
  riverpod_annotation: ^4.0.0
  freezed_annotation: ^3.1.0
  user_domain:
    path: ../user_domain
  common:
    path: ../common
  core:
    path: ../core
  ui_components:
    path: ../ui_components

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.0
  freezed: ^3.2.3
  riverpod_generator: ^4.0.0+1
